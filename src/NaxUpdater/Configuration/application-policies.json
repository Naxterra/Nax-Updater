{
  "applications": [
    {
      "id": "DeepL.DeepL",
      "displayName": "DeepL",
      "publisherContains": "DeepL",
      "blockedProviders": [ "winget-fallback" ],
      "managementMode": "ZeroInstall",
      "preferredProvider": "zero-install",
      "appliesWhenAbsent": false,
      "reason": "The installed app is content-addressed by its Zero Install feed, which takes precedence over the WinGet fallback."
    },
    {
      "id": "Nextcloud.NextcloudDesktop",
      "displayName": "Nextcloud",
      "publisherContains": "Nextcloud",
      "blockedProviders": [ "winget-fallback" ],
      "managementMode": "DirectVendor",
      "preferredProvider": "github:nextcloud-releases/desktop",
      "versionNormalization": "FirstThreeNumericComponents",
      "appliesWhenAbsent": false,
      "reason": "Use the executable for installed state and normalize the MSI build suffix to the public release version."
    },
    {
      "id": "Blizzard.BattleNet",
      "displayName": "Battle.net",
      "publisherContains": "Blizzard",
      "blockedProviders": [ "winget-fallback" ],
      "managementMode": "NativeSelfUpdater",
      "preferredProvider": "native-updater",
      "appliesWhenAbsent": false,
      "reason": "Battle.net owns its update lifecycle through Blizzard's installed updater; the WinGet fallback is blocked."
    },
    {
      "id": "Brave.BraveOrigin.Nightly",
      "displayName": "Brave Origin",
      "publisherContains": "Brave",
      "blockedProviders": [ "winget-fallback" ],
      "managementMode": "NativeSelfUpdater",
      "preferredProvider": "native-updater",
      "appliesWhenAbsent": false,
      "reason": "Brave Origin keeps its installed native update channel; the WinGet fallback cannot replace its channel."
    }
  ]
}
